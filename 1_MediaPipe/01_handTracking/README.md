# MediaPipe Hand Tracking - Pinch Detection

## 프로젝트 개요
MediaPipe Hands 라이브러리를 사용하여 웹캠으로 손을 추적하고, 엄지와 검지 손가락 사이의 핀치(집기) 동작을 실시간으로 감지하는 웹 애플리케이션입니다.

## 사용 기술
- **MediaPipe Hands**: Google의 손 추적 및 랜드마크 감지 라이브러리
- **HTML5 Canvas**: 비디오 및 손 랜드마크 시각화
- **JavaScript**: 실시간 처리 및 감지 로직
- **웹캠 API**: 실시간 비디오 스트림 캡처

## 주요 기능

### 1. 실시간 손 추적
- 웹캠을 통해 실시간으로 손을 감지하고 추적
- 최대 1개의 손 감지 (`maxNumHands: 1`)
- 21개의 손 랜드마크 포인트 추출

### 2. 손 랜드마크 시각화
- Canvas에 손의 모든 관절(랜드마크) 표시
- 손가락 뼈대(연결선) 그리기
- 640x480 해상도로 렌더링

### 3. 핀치 동작 감지
- 엄지 손가락 끝(4번 랜드마크)과 검지 손가락 끝(8번 랜드마크) 사이의 거리 계산
- 유클리드 거리 공식 사용: `√((x₁-x₂)² + (y₁-y₂)²)`
- 거리가 0.05 미만일 때 핀치 동작으로 감지
- 상태에 따라 UI 피드백 제공

## 코드 구조

### MediaPipe Hands 초기화
```javascript
const hands = new Hands({
    locateFile: (file) => {
        return `https://cdn.jsdelivr.net/npm/@mediapipe/hands/${file}`;
    }
});

hands.setOptions({
    maxNumHands: 1,              // 최대 손 개수
    modelComplexity: 1,          // 모델 복잡도 (0~2)
    minDetectionConfidence: 0.7, // 손 감지 최소 신뢰도
    minTrackingConfidence: 0.7   // 손 추적 최소 신뢰도
});
```

### 결과 처리 (onResults)
1. **캔버스 초기화**: 이전 프레임 지우기
2. **비디오 프레임 그리기**: 웹캠 영상을 캔버스에 렌더링
3. **손 랜드마크 그리기**: 감지된 손의 관절 및 연결선 표시
4. **핀치 감지**:
   - 엄지(4번)와 검지(8번) 좌표 추출
   - 거리 계산
   - 임계값(0.05) 비교
   - 상태 텍스트 및 색상 업데이트

### 카메라 설정
```javascript
const camera = new Camera(videoElement, {
    onFrame: async () => {
        await hands.send({ image: videoElement });
    },
    width: 640,
    height: 480
});
```

## 핀치 감지 알고리즘

### 거리 계산식
```
distance = √((thumbTip.x - indexTip.x)² + (thumbTip.y - indexTip.y)²)
```

### 임계값
- **핀치 감지**: distance < 0.05
- **일반 상태**: distance ≥ 0.05

### 상태 표시
- **핀치 감지 시**: "🤏 집기 동작 감지!" (초록색)
- **일반 상태**: "✋ 손을 보여주세요" (흰색)

## 손 랜드마크 인덱스
MediaPipe Hands는 손에서 21개의 랜드마크를 감지합니다:
- **0**: 손목 (Wrist)
- **1-4**: 엄지 (Thumb)
  - 4번: 엄지 끝
- **5-8**: 검지 (Index)
  - 8번: 검지 끝
- **9-12**: 중지 (Middle)
- **13-16**: 약지 (Ring)
- **17-20**: 새끼손가락 (Pinky)

## 실행 화면 
<video width="640" controls>
  <source src="demo.mp4" type="video/mp4">
  Your browser does not support the video tag.
</video> 
 

## 실행 방법
1. 웹 브라우저에서 `index.html` 파일 열기
2. 웹캠 권한 허용
3. 손을 카메라에 비추기
4. 엄지와 검지를 가까이 하여 핀치 동작 테스트

## 설정 옵션

### 감지 정확도 조정
- `minDetectionConfidence`: 0.0~1.0 (기본값: 0.7)
  - 높을수록 정확하지만 감지가 어려워짐
- `minTrackingConfidence`: 0.0~1.0 (기본값: 0.7)
  - 추적 안정성 조절

### 핀치 민감도 조정
```javascript
if (distance < 0.05) {  // 이 값을 조정하여 민감도 변경
    // 0.05 → 더 작게: 덜 민감 (손가락이 더 가까워야 감지)
    // 0.05 → 더 크게: 더 민감 (손가락이 멀어도 감지)
}
```

## 개선 가능 사항
- [ ] 다중 손 추적 지원
- [ ] 다른 제스처 감지 추가 (피스, 주먹, OK 사인 등)
- [ ] 핀치 강도 측정 (거리에 따른 비율 표시)
- [ ] 손 위치 좌표 실시간 표시
- [ ] 제스처 이력 기록 및 시각화
- [ ] 제스처 기반 인터랙션 구현 (버튼 클릭, 드래그 등)

## 라이브러리 의존성
```html
<!-- MediaPipe Hands -->
<script src="https://cdn.jsdelivr.net/npm/@mediapipe/hands/hands.js"></script>
<!-- Camera Utils -->
<script src="https://cdn.jsdelivr.net/npm/@mediapipe/camera_utils/camera_utils.js"></script>
<!-- Drawing Utils -->
<script src="https://cdn.jsdelivr.net/npm/@mediapipe/drawing_utils/drawing_utils.js"></script>
```

## 참고 자료
- [MediaPipe Hands 공식 문서](https://google.github.io/mediapipe/solutions/hands.html)
- [MediaPipe Hands JavaScript API](https://www.npmjs.com/package/@mediapipe/hands)